<h1>
    {{#if isEmptyQuery}}
        List of All Packages
    {{else}}
        Search results for "{{query}}"
    {{/if}}
</h1>

{{#if total}}
    Showing {{first}} to {{last}} of {{total}}.

    {{#if hasPrevious}}
        <a href="#" {{action previousPage}} rel="nofollow">Previous Page</a>
    {{/if}}
    {{#if hasNext}}
        <a href="#" {{action nextPage}} rel="nofollow">Next Page</a>
    {{/if}}

    <ol {{bind-attr start=first}}>
        {{#each hits}}
        <li>
            <div>
                {{view App.PackageIcon contentBinding="this"}}
                {{#link-to 'packages.view' this}}
                {{#if title}}
                    {{title}}
                {{else}}
                    {{id}}
                {{/if}}
                {{/link-to}}
            </div>
            <div>
                By:
                {{#each author in authors}}
                    {{#link-to 'packages.search' author rel="nofollow"}}{{author}}{{/link-to}}
                {{/each}}
            </div>
            <div>
                {{description}}
            </div>
            <div>
                Tags:
                {{#each tag in tags}}
                    {{#link-to 'packages.search' tag rel="nofollow"}}{{tag}}{{/link-to}}
                {{/each}}
            </div>
        </li>
        {{/each}}
    </ol>

    {{#if hasPrevious}}
        <a href="#" {{action previousPage}} rel="nofollow">Previous Page</a>
    {{/if}}
    {{#if hasNext}}
        <a href="#" {{action nextPage}} rel="nofollow">Next Page</a>
    {{/if}}
{{else}}
    No results :'(
{{/if}}
